import type { Metadata } from 'next';
import { LocaleRedirect, Shell } from '../components';
import { profiles, site } from '@/lib/site';
export const metadata: Metadata = { title: 'Feyn — Arch & Build', description: 'Feyn on architecture, engineering, AI products, and the work of building.', alternates: { canonical: '/en/', languages: { 'zh-CN': '/', en: '/en/' } } };
export default function EnglishHome() { const profile = profiles.en; return <Shell locale="en"><LocaleRedirect locale="en" /><section className="hero"><p className="eyebrow">{profile.eyebrow}</p><h1>Arch &amp; <em>Build</em></h1><p className="intro">{profile.intro}</p><div className="link-row">{site.links.map((link) => <a key={link.href} href={link.href} target={link.href.startsWith('http') ? '_blank' : undefined} rel="noreferrer">{link.label}</a>)}</div></section><section className="statement">{profile.statements.map((statement) => <p key={statement}>{statement}</p>)}<a className="text-link" href="/en/resume/">{profile.resumeCta} ↗</a></section></Shell>; }

import { blog } from '@/lib/site';

const escape = (value: string) => value.replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;');
export function GET() { const items = blog.zh.map((entry) => `<item><title>${escape(entry.title)}</title><link>https://feyn.cc/blog/${escape(entry.slug)}/</link><guid>https://feyn.cc/blog/${escape(entry.slug)}/</guid><description>${escape(entry.description)}</description>${entry.publishedAt ? `<pubDate>${new Date(entry.publishedAt).toUTCString()}</pubDate>` : ''}</item>`).join(''); return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Feyn — Blog</title><link>https://feyn.cc/blog/</link><description>架构、工程与 AI 产品实践。</description><language>zh-CN</language><atom:link xmlns:atom="http://www.w3.org/2005/Atom" href="https://feyn.cc/rss.xml" rel="self" type="application/rss+xml"/>${items}</channel></rss>`, { headers: { 'content-type': 'application/rss+xml; charset=utf-8' } }); }
